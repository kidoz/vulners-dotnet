<Project>
  <!--
    Shared build settings for all projects in the solution.
    Warnings are treated as errors everywhere to keep the build clean.
  -->
  <PropertyGroup>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
</Project>
